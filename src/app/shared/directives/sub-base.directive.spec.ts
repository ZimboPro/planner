import { SubBaseDirective } from './sub-base.directive';

describe('SubBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new SubBaseDirective();
    expect(directive).toBeTruthy();
  });
});
